---
date: 2026-02-25 15:59:17
category: UI
session_id: e32d1528-0e8a-4435-bf29-eb53b9296ace
tools_used: none
cwd: C:\Users\bcchoi\Desktop\01.Project\106.디자인 시안\02.Material
---

# Prompt

이것 처럼 이나 색상에서 관리되지 않고 따로 처리되는 것들이 있는지 다시 검토해

---

# Response

전체 검토 완료. 스타일 딕셔너리 밖에서 관리되는 항목들을 정리합니다.

---

## 검토 결과 — 스타일 딕셔너리 외부 처리 현황

### 1. XAML 하드코딩 컬러 (CRITICAL)

**`MainWindow.xaml` — StatusBar 연결 상태 아이콘**
| 라인 | 코드 | 수정 방향 |
|------|------|----------|
| ~256 | `Foreground="#FF4CAF50"` + DropShadow | `{DynamicResource SuccessBrush}` |
| ~272 | `Foreground="#FFF44336"` + DropShadow | `{DynamicResource ErrorBrush}` |
| ~290 | `Foreground="#FFFFC107"` + DropShadow | `{DynamicResource WarningBrush}` |

**`OscilloscopeDialog.xaml` — 채널 체크박스 색상**
| 라인 | 코드 | 수정 방향 |
|------|------|----------|
| ~82 | `Foreground="#FFFFEB3B"` CH1 | Colors.xaml에 `ChartCH1Brush` 토큰 추가 |
| ~87 | `Foreground="#FF4CAF50"` CH2 | `ChartCH2Brush` |
| ~92 | `Foreground="#FF29B6F6"` CH3 | `ChartCH3Brush` |
| ~97 | `Foreground="#FFEF5350"` CH4 | `ChartCH4Brush` |

### 2. C# 코드 직접 브러시 생성 (CRITICAL)

**`MainWindow.xaml.cs` — `UpdateThemeColors()` (Hue 슬라이더)**
- `res["PrimaryBrush"] = new SolidColorBrush(...)` 등 **8개 리소스키** 직접 할당
- Hue 슬라이더는 동적 계산이라 코드 처리가 불가피하지만, 현재 구조는 개선 가능

**`OscilloscopeDialog.xaml.cs` — ScottPlot 차트 색상**
- `#1E1E1E`, `#252526`, `#3C3C3C`, `#424242` 등 **13개** 하드코딩 hex

### 3. i18n 위반 (40개 이상)

**`MainWindow.xaml`** — Ribbon 헤더/버튼 라벨 전부 하드코딩
- `Header="File"`, `Header="Tools"`, `Header="Communication"` 등

**`OscilloscopeDialog.xaml`** — 전체 텍스트 하드코딩
- `Text="Time/Div:"`, `Content="Auto"`, `Content="CH1 Position"` 등

---

바로 수정 작업을 진행할까요? 우선순위는:

1. **XAML 하드코딩 컬러** → DynamicResource로 교체 (즉시 가능)
2. **ScottPlot 차트 색상** → Colors.xaml 토큰 추가 + 코드에서 참조
3. **i18n** → 전체 작업 규모가 크므로 별도 진행 추천