---
date: 2026-02-24 16:06:54
category: UI
session_id: 4e43d4e6-8022-4d0b-8341-00cabee922e1
tools_used: none
cwd: C:\Users\bcchoi\Desktop\01.Project\106.디자인 시안\02.Material
---

# Prompt

1. 글씨 크기는 폰트 사이즈로 스케일 바 아니고 그냥 숫자에서 드롭다운 메뉴로 선택 할 수 있도록 
그리고 또 스케일 바같은거 등록 할 때도 지금 색상 코드 하나도 안지키고 있네, 리본에 등록하는 메뉴들은 전부 색상 스타일 지켜, 글자 크기랑 배치 위치, 폰트도 이 내용 스킬이랑 에이전트랑 클로드.md에도 등록해

---

# Response

C# 컴파일 에러 0개 — 코드는 정상입니다. exe 파일 잠금만 문제입니다.

앱을 수동으로 닫아주시면 빌드+실행이 가능합니다. 변경 내용 정리:

### 수정 내역

**1. Font Size: 슬라이더 → 드롭다운**
- Views > Font Size 그룹: 슬라이더 제거 → ComboBox (8, 9, 10, 11, 12, 13, 14, 16, 18, 20)
- ViewModel: `FontScalePercent` → `SelectedFontSize` (기본값 12)
- 메시지: `FontScaleChangedMessage` → `FontSizeChangedMessage`

**2. 리본 인라인 컨트롤 디자인 토큰 전면 적용**

| 위치 | 수정 전 (위반) | 수정 후 (준수) |
|------|-------------|-------------|
| Connection > Port 라벨 | raw `Foreground` + `FontSize` + `Margin` | `Style="{StaticResource RibbonInlineLabel}"` |
| Connection > Port 패널 | `Margin="4,2"` | `Margin="{StaticResource Padding.RibbonInline}"` |
| Options > Hue 라벨 | raw `Foreground` + `FontSize` | `Style="{StaticResource RibbonInlineLabel}"` |
| Options > Hue 패널 | `Margin="4,2"` | `Margin="{StaticResource Padding.RibbonInline}"` |
| Options > Preview rect | `Width="20" Height="20"` | `{StaticResource Size.IconLG}` |
| Views > Font Size | raw slider + raw labels | ComboBox + `RibbonInlineLabel` + `RibbonDarkComboBox` |

**3. 새 토큰 (Styles.xaml)**
- `Padding.RibbonInline` — 리본 인라인 패널 여백
- `Margin.RibbonLabelGap` — 라벨-컨트롤 간 간격
- `RibbonInlineLabel` 스타일 — 리본 내 라벨 전용

**4. 규칙 등록**
- `CLAUDE.md` → "Ribbon Controls (MUST follow)" 섹션 추가
- `material-wpf-generator/SKILL.md` → "Ribbon Controls" 규칙 추가
- `ui-dev/SKILL.md` → 리본 인라인 필수 스타일 목록 추가
- `MEMORY.md` → 사용자 요구사항 기록