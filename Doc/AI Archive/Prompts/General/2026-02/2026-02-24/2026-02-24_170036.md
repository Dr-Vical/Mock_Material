---
date: 2026-02-24 17:00:36
category: General
session_id: 4e43d4e6-8022-4d0b-8341-00cabee922e1
tools_used: none
cwd: C:\Users\bcchoi\Desktop\01.Project\106.디자인 시안\02.Material
---

# Prompt

1. 파라메터 항목은 파라메터 폴더에 있는 csv 파일을 읽어서 그것을 토대로 작성 
2. Enable Disable 이부분은 리본메뉴에 항상 떠있어야 돼 그래서 리본 메뉴에서 한단 내려와 있는데 그걸 리본메뉴 쪽으로 올려줘. 리본 메뉴를 좀 축소해도 되니까
3. Exit 오른쪽 구분자는 빼 , Exit 하단 Appli 이 문구도 빼고
4. 각 아이콘 + 아이콘 글씨는 3포인트 정도 하단으로 내려오고 그아래 File / Print 이 부분은 3포인트 정도 올라오고 리본 메뉴 사이즈도 3포인트 정도 줄여줘

---

# Response

앱이 정상 실행되었습니다. 4가지 변경 사항을 모두 구현 완료했습니다.

### 구현 요약

**1. CSV 파라메터 로딩**
- `Services/CsvParameterLoader.cs` 생성 — `Parameters/CSD7N_Parameter.csv`에서 파라메터 로드
- NodeType → CSV Tree 컬럼 매핑 (예: `"PIDTuning"` → `"PID Tuning"`, `"Group0"` → `"Group/Group 0 : Basic"`)
- RFC 4180 CSV 파서 (쿼트 내 쉼표 처리)
- `MainWindowViewModel.cs`의 하드코딩된 switch/case 전체 제거, CSV 기반 로딩으로 교체

**2. Enable/Disable All → 리본 메뉴 안으로 이동**
- 리본 아래 별도 Quick Actions 바(Border) 제거
- Grid 오버레이로 리본 콘텐츠 영역 우측에 배치 (`VerticalAlignment="Bottom"`, `Margin="0,0,8,20"`)
- 탭 전환과 무관하게 항상 표시

**3. Exit 우측 구분자/Application 헤더 제거**
- `Header="Application"` → `Header=" "` (빈 문자열로 헤더 숨김)

**4. 리본 스페이싱 조정**
- `RibbonLargeRipple`/`RibbonLargeRippleToggle`: Padding `8,4` → `8,7,8,1` (아이콘 3pt 하단), MinHeight `56` → `53` (전체 3pt 축소)
- `RibbonButtonLabel`: Margin `0,2,0,0` → `0,4,0,0` (텍스트 하단 여백 증가)