---
date: 2026-02-24 15:48:49
category: General
session_id: 4e43d4e6-8022-4d0b-8341-00cabee922e1
tools_used: none
cwd: C:\Users\bcchoi\Desktop\01.Project\106.디자인 시안\02.Material
---

# Prompt

화면이 커져도 각 셀들의 간격은 글자 최대 길이로 맞춰주고 (너무 커지지 않게) 
줄어든다면 FT NUM / PARAMETER / VAlue를 최대한 유지하고 뒤쪽에 Unit이나 Default나 이런 부분들을 먼저 줄여줘, 그리고 그 세개도 침범 받을 때는 글씨 크기를 조금씩 줄여서 최대한 글씨가 보이게 해주고, 다시 넓어지면 본래 글씨 크기 로 가변 시켜줘

---

# Response

컴파일 자체는 성공했지만 RswareDesign.exe가 실행 중이라 파일 복사가 안 되는 것입니다 (프로세스 31392). 코드 에러는 없습니다.

앱을 닫고 다시 빌드하면 됩니다. 변경 내용 요약:

### 1. 반응형 DataGrid 컬럼 (`ParameterEditorView`)

**3단계 레이아웃 알고리즘:**

| 단계 | 조건 | 동작 |
|------|------|------|
| Comfortable | 공간 충분 (≥840px) | 모든 컬럼 = 글자 최대 길이 (고정, 안 늘어남) |
| Squeeze P1 | 공간 부족 | UNITS, DEFAULT, MIN, MAX 비례 축소 → 30px 미만시 숨김 |
| Squeeze P0 | 매우 부족 | P1 완전 숨김 + FT NUM/PARAMETER/VALUE 축소 + **폰트 크기 72%까지 감소** |

- 넓어지면 원래 폰트 크기로 자동 복귀
- 모든 셀에 `TextTrimming="CharacterEllipsis"` 적용 (잘린 텍스트 "..." 표시)

### 2. 글씨 사이즈 일괄 변경 (`Views` 탭)

- **Views > Font Size** 그룹에 Scale 슬라이더 추가 (70% ~ 150%, 10% 단위)
- 슬라이더 변경 시 모든 FontSize 토큰 (XS~3XL) 일괄 업데이트
- ParameterEditorView는 `FontScaleChangedMessage`를 받아 반응형 컬럼도 연동