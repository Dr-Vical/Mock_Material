---
date: 2026-02-26 09:25:25
category: General
session_id: 8d59fe6a-e8e1-44db-ab06-368aabcd4c0d
tools_used: none
cwd: C:\Users\bcchoi\Desktop\01.Project\106.디자인 시안\02.Material
---

# Prompt

1. 뷰에서 이 기능은 빼자 
2. 오른쪽 버튼 바를 MAX 뒤에 붙여줘
3. Drive 하위 Mode Config 아래 Drive 소속 트리 하나 만들자. 이름은 즐겨찾기 (영문으로)
4. 그리고 FT Num 제일 앞에 별표 모양 버튼들을 노란색으로 누를수 있는 즐겨 찾기를 만들고, 해당 아이템이 눌릴경우 위 즐겨찾기 트리항목에 추가될 수 있게 해줘

---

# Response

4가지 수정 완료:

### 1. Spacing 드롭다운 제거
- 리본 UI + ViewModel + Message + Handler 모두 정리

### 2. 액션 버튼 바 → MAX 뒤 밀착
- 별도 `SurfaceVariantBrush` 배경 제거 → `SurfaceBrush`로 통일
- `Width="100"` 고정 폭 제거, `Padding="4,0"`으로 최소 간격
- DataGrid와 자연스럽게 이어지는 구조

### 3. Favorites 트리 노드
- Drive > Mode Configuration 바로 아래 `Favorites` (별 아이콘) 노드 추가
- `IsExpanded=true`로 기본 열림 상태

### 4. 별표 즐겨찾기 기능
- FT NUM 앞에 `★` 컬럼 (30px) 추가 — 노란색 ToggleButton
- 미선택: `StarOutline` + `TextDisabled` 색상
- 선택: `Star` + `WarningBrush` (노란색)
- 클릭 시 Parameter의 `IsFavorite` 토글 → ViewModel이 `PropertyChanged` 감지 → Favorites 트리에 자동 추가/제거