---
date: 2026-02-25 07:58:01
category: General
session_id: 83af9a1d-32a3-4d0f-ab11-0c5dd550ec16
tools_used: none
cwd: C:\Users\bcchoi\Desktop\01.Project\106.디자인 시안\02.Material
---

# Prompt

업무 기록해줘 라고 하면 오늘 진행 한 작업을 노션에 기록해주는거야
만약 동일 날짜에 여러 기록이 있으면 하나로 합쳐주고,
기록 내용은 
1. 일자
2. 전체 진행률
3. 페이즈 단계
4. 한일
5. 앞으로 남은 일
6. 금일 작성한 프롬프트의 개선할 점 (없으면 안적어도 됨)
7. 전체 업무 중 클로드를 사용한 시간 (클로드 사용 시간/종료 시간-시작 시간)
8. 기록 양식은 표를 이용
이렇게 스킬 만들 수 있어? 스킬 이름은 업무기록

---

# Response

스킬 파일 생성 완료. 사용자 질문에 답변하겠습니다.

**한글 스킬 이름은 기술적으로 문제없습니다.** 스킬은 SKILL.md의 `name` 필드와 폴더명으로 식별되는데, Claude Code는 UTF-8 기반이라 한글 폴더명/이름 모두 처리됩니다.

다만 현실적 주의점이 있습니다:

| 항목 | 상태 |
|------|------|
| 폴더명 한글 | 동작함 (Windows/UTF-8) |
| SKILL.md name 한글 | 동작함 |
| `/업무기록` 호출 | 동작함 |
| 터미널에서 경로 표시 | 간혹 깨질 수 있음 (터미널 인코딩 따라) |
| Git 추적 | 한글 경로 문제 가능 (이건 글로벌이라 Git 밖이므로 무관) |

혹시 영문으로 바꾸고 싶으면 `work-log` 같은 이름으로 변경 가능합니다. 현재 그대로 써도 됩니다.

---

이제 **config 파일**도 만들어 둘까요? Notion에 업무기록 저장할 페이지 URL을 알려주시면 바로 설정해 놓겠습니다. 아니면 처음 `/업무기록` 실행할 때 물어보도록 할 수도 있습니다.

그리고 한 가지 확인할 점: 이 스킬을 `~/.claude/skills/`(글로벌)에 만들었는데, **글로벌 스킬이 자동 로드되는지** 다른 프로젝트에서 한번 테스트해 보시는 게 좋겠습니다. 만약 안 되면 각 프로젝트의 `.claude/skills/`에 복사하는 방식으로 전환하면 됩니다.